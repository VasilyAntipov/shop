import { } from "../constants"

const initState = {
    isAuth: false,
    user: {},


}

export const userReducer = (state = initState, action) => {
    switch (action.type) {

        default:
            return state
    }
}
